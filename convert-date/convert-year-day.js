'use strict';

/**
 * Convert years to days.
 *
 * @param {number} years - The years.
 * @returns {number} - Converting years to date.
 */

export const convertDate = (years = 0) => {
    return years * 365;
};
